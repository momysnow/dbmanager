# Direct (top-level) dependencies. Keep ranges loose here so future bumps
# stay readable. Production builds DO NOT install from this file — they
# install from requirements.lock, which pins every transitive dep to the
# exact version that was resolved when the lock was last regenerated.
# Regenerate the lock after editing this file (see header of requirements.lock).

# Database (async SQLAlchemy + Alembic)
sqlalchemy[asyncio]>=2.0
aiosqlite
alembic
sqlparse>=0.4

# Core dependencies
typer
InquirerPy
rich
pydantic
python-crontab

# API
fastapi
uvicorn[standard]
python-multipart
# requests is often needed for client calls/notifications
requests

# Database drivers
psycopg2-binary
pymysql>=1.1.0
pymssql>=2.3.13

# Cloud storage
boto3>=1.34.0
docker

# Utilities
cryptography
zstandard
python-jose[cryptography]
passlib[argon2]
argon2-cffi
smbprotocol
